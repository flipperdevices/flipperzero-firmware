# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="gas_sensor",  # Must be unique
    name="Gas Sensor",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="gas_sensor_app",
    stack_size=2 * 1024,
    fap_category="GPIO",
    # Optional values
    # fap_version="0.1",
    fap_icon="gas_sensor.png",  # 10x10 1-bit PNG
    # fap_description="A simple app",
    # fap_author="J. Doe",
    # fap_weburl="https://github.com/user/gas_sensor",
    fap_icon_assets="images",  # Image assets to compile for this application
)
