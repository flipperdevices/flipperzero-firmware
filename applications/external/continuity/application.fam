# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="continuity",  # Must be unique
    name="Continuity",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="continuity_app",
    stack_size=2 * 1024,
    fap_category="GPIO",
    # Optional values
    fap_version="1.0",
    fap_icon="continuity.png",  # 10x10 1-bit PNG
    # fap_description="A simple app",
    fap_author="Ben Allen",
    # fap_weburl="https://github.com/user/continuity",
    fap_icon_assets="images",  # Image assets to compile for this application
)
