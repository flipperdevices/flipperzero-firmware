App(
    appid="uhf_rfid",
    name="[UHF]RFID",
    apptype=FlipperAppType.EXTERNAL,
    targets=["f7"],
    entry_point="uhf_app_main",
    requires=[
        "storage",
        "gui",
    ],
    stack_size=4 * 1024,
    order=30,
    # fap_icon="125_10px.png",
    fap_category="RFID",
    # fap_libs=["mbedtls"],
    # fap_private_libs=[
    #     Lib(
    #         name="loclass",
    #     ),
    # ],
    # fap_icon_assets="icons",
)
