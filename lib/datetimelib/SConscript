Import("env")

env.Append(
    LINT_SOURCES=[
        Dir("."),
    ],
    CPPPATH=[
        "#/lib/datetimelib",
    ],
    SDK_HEADERS=[
        File("datetimelib.h"),
    ],
)

libenv = env.Clone(FW_LIB_NAME="datetimelib")
libenv.ApplyLibFlags()

sources = libenv.GlobRecursive("*.c*")

lib = libenv.StaticLibrary("${FW_LIB_NAME}", sources)
libenv.Install("${LIB_DIST_DIR}", lib)
Return("lib")
