Import("env", "lib_flags")

env.Append(
    CPPPATH=[
        "#/lib/drivers",
    ],
)


libenv = env.Clone()
libenv.MergeFlags(lib_flags)

sources = Glob("*.c", source=True)

lib = libenv.StaticLibrary("hwdrivers", sources)
libenv.Install("${LIB_DIST_DIR}", lib)
Return("lib")
