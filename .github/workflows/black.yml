name: Python Lint

on: 
  push:
    paths:
      - '**.py'
  pull_request:
  
jobs:
  lint:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: psf/black@stable
